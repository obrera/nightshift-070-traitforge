services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      PORT: 3000
    restart: unless-stopped
    volumes:
      - traitforge-data:/app/data

volumes:
  traitforge-data:
